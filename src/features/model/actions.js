import { setColorsetId, setTemplateId } from '../../entities/template/model/template.slice.js';

export const loadTemplate = (websiteTemplate) => {
  // return async (dispatch, getState) => {
  //   const response = await fetch(`../../../../layouts/test/test.html`);
  //   const html = await response.text();
  //
  //   // const cssResponse = await fetch(`../../../../layouts/${websiteTemplate}/index.css`);
  //   // const css = await cssResponse.text();
  //
  //   dispatch(setTemplateId(html));
  //   // dispatch(setColorsetId(css));
  // };
};